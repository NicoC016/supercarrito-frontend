import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecommerce.app',
  appName: 'e-commerce-proyect',
  webDir: 'dist/e-commerce-proyect',
  bundledWebRuntime: false,
};

export default config;
